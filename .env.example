VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------
# Server-side only (Netlify Functions) — NOT read by the Vite client
# build (no VITE_ prefix, on purpose). Set these in Netlify → Site
# settings → Environment variables, never in this file with real
# values, and never commit real secrets here.
# ---------------------------------------------------------------------
# SUPABASE_URL=                    # same value as VITE_SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY=       # Supabase → Project Settings → API → service_role secret
# RESEND_API_KEY=                  # resend.com → API Keys
# RESEND_FROM_EMAIL=               # e.g. "Monad Africa <events@yourdomain.com>" (or onboarding@resend.dev for testing)
#
# EVENT_OTP_PEPPER=                # optional extra secret mixed into the
#                                   # event-verification OTP hash (see
#                                   # netlify/functions/_lib/eventVerifyShared.ts).
#                                   # Defense-in-depth on top of the
#                                   # per-code random salt — safe to leave
#                                   # unset, but recommended in production
#                                   # (any long random string). Used by
#                                   # netlify/functions/event-verify-request.ts
#                                   # and event-verify-confirm.ts, which
#                                   # also reuse SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY/
#                                   # RESEND_API_KEY/RESEND_FROM_EMAIL above.
#
# Password reset (src/pages/ForgotPassword.tsx) uses
# netlify/functions/password-reset-request.ts to request the code —
# reusing the same SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY/
# RESEND_API_KEY/RESEND_FROM_EMAIL above — instead of Supabase's
# resetPasswordForEmail(), because Supabase only lets you edit its
# built-in "Reset Password" email template once Custom SMTP is enabled
# on the project, and this project deliberately isn't doing that. The
# function mints a real Supabase recovery OTP via the Admin API and
# emails it directly via Resend; verification still goes straight
# through supabase.auth.verifyOtp({ type: 'recovery' }) from the
# client, same as native Supabase OTP recovery. Also run
# supabase/migrations/0034_password_reset_request_cooldown.sql once —
# it adds the table this function uses to enforce a per-email resend
# cooldown (the Admin API has no built-in rate limit of its own).
