VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------
# Event Registration & Invite Verification — server-side only.
# These are NOT read by the Vite client build (no VITE_ prefix, on
# purpose) — they're consumed exclusively by netlify/functions/send-invite-email.ts
# at runtime. Set them in Netlify → Site settings → Environment
# variables, never in this file with real values, and never commit
# real secrets here.
# ---------------------------------------------------------------------
# SUPABASE_URL=                    # same value as VITE_SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY=       # Supabase → Project Settings → API → service_role secret
# RESEND_API_KEY=                  # resend.com → API Keys
# RESEND_FROM_EMAIL=               # e.g. "Monad Africa <events@yourdomain.com>" (or onboarding@resend.dev for testing)
# SITE_URL=                        # optional, defaults to https://monadafricans.netlify.app
